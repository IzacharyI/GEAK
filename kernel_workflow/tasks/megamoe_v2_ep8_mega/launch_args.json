{
  "_comment": "MEGA-MODE launch configuration for MegaMoE V2 EP8. Every candidate uses the same tiled-megakernel Analyze/Plan/Author/Verify lifecycle. A matched Expert Skill supplies normative knowledge but never creates a separate reproduction mode. Every candidate is measured against the same frozen public-AITER denominator and the fastest fully verified candidate is selected. ${...} placeholders are resolved by scripts/bootstrap_task.sh.",

  "mode": "mega",
  "_mode_comment": "mode=mega selects tiled/instruction-pipelined megakernel authoring. use_expert_skills changes knowledge only; it does not change roles, scheduling, lifecycle, or acceptance. MODE!=='mega' paths remain unchanged.",

  "kernel_path": "${WORKSPACE}",
  "workflow_dir": "${SKILL_DIR}",
  "exp_root": "${EXP_ROOT}",
  "state_dir": "${STATE_DIR}",

  "budget": 6,
  "max_no_improve": 3,
  "min_improve": 0.01,
  "objective": "speedup",
  "mega_profile": "production",
  "mega_fast_test": "true",
  "mega_stop_on_deliverable": "true",
  "mega_min_search_attempts": 2,
  "mega_candidate_timeout_s": 3600,
  "mega_final_timeout_s": 7200,
  "mega_time_budget_s": 28800,
  "mega_final_reserve_s": 7200,
  "mega_closeout_reserve_s": 900,
  "_objective_comment": "Mega uses one candidate lifecycle for all knowledge configurations. Candidates may fully or partially fuse stages, but must implement tile/instruction scheduling, remain a complete runnable operator, and beat the frozen baseline on rank-max operator_e2e.",
  "expert_skill_id": "megamoe_ep_mega_fusion",
  "expert_skill_revision": "m25-v3",
  "expert_skill_baseline_commit": "8775229e003af030abea7c50d2ed9e956311843d",
  "expert_skill_accuracy_cases": ["128", "512", "8192"],
  "expert_skill_playbook": "${SKILL_DIR}/../perf_knowledge/expert_skills/skills/megamoe_ep_mega_fusion/playbook.md",
  "expert_skill_contract": "${SKILL_DIR}/../perf_knowledge/expert_skills/skills/megamoe_ep_mega_fusion/contract.yaml",
  "expert_skill_validation": "${SKILL_DIR}/../perf_knowledge/expert_skills/skills/megamoe_ep_mega_fusion/validation.yaml",
  "verify_expert_skill_contract": "true",
  "require_expert_skill_contract": "false",
  "expert_skill_contract_tool": "${SKILL_DIR}/tools/expert_skill_contract.py",
  "expert_skill_reference_path": "",
  "mega_graph_contract_tool": "${SKILL_DIR}/tools/mega_graph_contract.py",
  "expert_skill_source_files": [
    "aiter/ops/flydsl/kernels/communication_ops_utils.py",
    "aiter/ops/flydsl/kernels/flydsl_dispatch_combine_intranode_kernel.py",
    "aiter/ops/flydsl/kernels/flydsl_dispatch_combine_intranode_op.py",
    "aiter/ops/flydsl/kernels/mega_moe/gemm1.py",
    "aiter/ops/flydsl/kernels/mega_moe/gemm2.py",
    "aiter/ops/flydsl/kernels/mega_moe/gemm_util.py",
    "aiter/ops/flydsl/kernels/mega_moe/mega_moe_config.py",
    "aiter/ops/flydsl/kernels/mega_moe/mega_moe_stage1.py",
    "aiter/ops/flydsl/kernels/mega_moe/mega_moe_stage2.py",
    "aiter/ops/flydsl/kernels/mega_moe/mega_moe_v2.py"
  ],
  "mega_final_top_k": 1,
  "mega_final_fallback_k": 2,
  "mega_tie_noise_pct": 0.0,
  "expert_skill_recorded_score": 1.0448,
  "expert_skill_recorded_low": 1.0403,
  "expert_skill_recorded_high": 1.0477,
  "_audit_profile_overrides": "For an exhaustive audit set mega_profile=audit, budget=15, mega_stop_on_deliverable=false, mega_candidate_timeout_s=3600, mega_final_timeout_s=7200, mega_time_budget_s=28800, mega_final_reserve_s=7200, mega_closeout_reserve_s=900, mega_final_top_k=2, mega_final_fallback_k=2, require_overlap=true, require_attribution=true, and both 512 required_pairs entries to 16.",
  "strict_autonomy": "false",
  "promotion_metric": "operator_e2e",
  "target_guards": ["8192_uniform"],
  "regression_guards": [
    "8192_rank-mixed-skew",
    "512_uniform",
    "512_rank-mixed-skew"
  ],
  "launch_target": 2,
  "require_overlap": "false",
  "require_attribution": "false",
  "require_artifact_distinct": "true",
  "_require_artifact_distinct_comment": "ON for measurement integrity: candidate and baseline arms must resolve to different JIT artifacts. This is not a plagiarism check and does not compare against M2.5 source, which is not an input.",
  "require_hardware_activation": "true",
  "_require_hardware_activation_comment": "ON (same as optimize). A switched / perf-bearing candidate is committable and counts as on-device progress ONLY if the verifier reports activation_on_hardware=yes -- the switched path traced+launched on a card THIS round (a gpu_lock benchmark/correctness with the switch set, backed by a device-side observable: nonzero mega_e2e, a rocprof/trace record, or the on-device path marker). A py_compile, a COMPILE_ONLY ISA hash, or a CPU dry-run is NOT hardware activation. Three consecutive rounds in which nothing reaches the device hard-stop the wave. In mega mode this is the primary guard against the last wave's failure: the whole-topology body must reach the card and clear the r12 cut4 boundary, not defer a trace-time fault across rounds.",
  "accuracy_metric": "relL2",
  "accuracy_threshold": 0.10,
  "required_replays": 256,
  "required_pairs": 5,
  "required_pairs_by_guard": {
    "8192_uniform": 5,
    "8192_rank-mixed-skew": 5,
    "512_uniform": 8,
    "512_rank-mixed-skew": 8
  },
  "_acceptance_comment": "PRODUCTION score tier requires hardware activation, a candidate-path marker on all ranks, relL2<0.10, 30 clean replays and paired 8192_uniform rank-max timing. Full fusion targets two launches; a search candidate may score as a complete partial fusion when its launch count is reduced versus baseline and speedup is >1.0. The selected candidate pays all regression guards, arrival-jittered 256 replays, graph/artifact/exact-head checks.",
  "_state_comment": "STATE persists every candidate independently under the same authoring and verification lifecycle.",

  "gpu_ids": "0,1,2,3,4,5,6,7",
  "gpus_per_job": 8,
  "_gpu_comment": "This is a collective: the group form of the lease takes all 8 cards as ONE lease. Candidate turns receive 3600s and full finalist validation 7200s; any uncancellable timeout stops the invocation to preserve single-writer safety.",
  "gpu_min_free_gib": 150,

  "use_expert_skills": "true",
  "_use_expert_skills_comment": "When true, the matched MegaMoE skill and playbook are advisory context for the same Mega Search Lead and Engineer used when false. Set false for a clean no-M2.5-knowledge capability run.",
  "analysis_skill": "none",
  "_analysis_skill_comment": "Pre-candidate Mega analysis is source/task-graph only. Fused-native measurements are collected only after a candidate exists; the frozen public bench cannot provide those fields.",
  "capability_eval": "false",
  "_capability_eval_comment": "FALSE: this run is for its OUTPUT. A separate fresh-state experiment may set use_expert_skills=false to evaluate derivation without M2.5 knowledge.",
  "known_reference_paths": "",
  "_known_reference_paths_comment": "Empty. No hand-authored M2.5 source address is an input; candidates are authored from the public baseline, with optional Expert Skill knowledge only.",

  "positive_control": {
    "name": "synthetic slowdown: calibrated device-side delay on the measured operator stream",
    "how": "Build the control in an isolated /tmp copy; never touch a candidate lane. Measure >=8 base-vs-base null pairs first. Add a bounded device-side delay that executes on the measured operator stream for every EP rank and is visible in the same rank-max span. Prefer an env/JIT-keyed delay at the persistent-kernel entry across resident blocks; if that location saturates below the required effect, use a dedicated tiny calibration kernel immediately inside the measured span. Do not reuse the old Stage2-local spin that maxed below the noise floor. Anchor the switch in the JIT cache key, record distinct disk/ISA hashes, and verify a device trace contains the delay. Dose it until >=5 interleaved pairs unanimously produce -4%..-9% and clear 3x the worst null pair. Write the aggregate and evidence manifest atomically; claim_complete stays false until all pairs and hashes are final.",
    "expected_pct_lo": -9.0,
    "expected_pct_hi": -4.0,
    "magnitude": "constructed",
    "_magnitude_comment": "REQUIRED. The band above is a TARGET a synthetic knob was aimed at, not a previously measured fact. With magnitude=constructed the gate still requires the right sign, unanimity across pairs, and >=3x the WORST null pair -- it only tolerates landing above half the target. Do NOT relabel a recorded control as constructed to get past a failure.",
    "implausible_pct": 25.0,
    "guard": "8192_uniform",
    "required_control_pairs": 5,
    "required_null_pairs": 8,
    "abort_on_fail": false,
    "_abort_on_fail_comment": "A failed or incomplete control does not abort candidate authoring; it keeps performance calibration pending, so no candidate can be ranked or shipped until a complete control passes. claim_complete prevents force-emitted 0.00% from being read as failure.",
    "_control_strength_comment": "The benchmark engineer must construct a stronger device-side delay than the old Stage2-local spin if that knob cannot clear the null spread. Null-only is not a positive control."
  },
  "require_task_graph": true,
  "_require_task_graph_comment": "Analyze must still emit the tile-level dependency DAG as a structured artifact (nodes, edges, edge scope, what enforces each edge today, critical path, slack, declared unknowns) before the one-shot topology is committed -- see knowledge/tile_task_graph.md and the ANALYZE_SCHEMA task_graph field. In mega mode this justifies WHICH edges the whole-topology design fuses (and guards against 'the stages are serialized, so fuse them' restatements); it is not a derivation-from-blind requirement. The gate does not abort; it emits a caveat that travels to the report."
}
