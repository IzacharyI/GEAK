{
  "_comment": "Portable launch recipe for the MegaMoE V2 EP8 task. This is a TEMPLATE: ${...} placeholders are resolved by scripts/bootstrap_task.sh, which writes the concrete file into the workspace it assembles. Nothing here points at a reference implementation, a prior run tree, or an accumulated patch -- the only inputs are a baseline AITER checkout, this workflow, and the machine's runtime libraries. If you are tempted to add a path to a previous wave's artifacts, read knowledge/../roles/benchmark_engineer.md 5b first: the answer-applied workspace IS the leak.",

  "kernel_path": "${WORKSPACE}",
  "workflow_dir": "${SKILL_DIR}",
  "exp_root": "${EXP_ROOT}",
  "state_dir": "${STATE_DIR}",

  "budget": 15,
  "max_no_improve": 3,
  "min_improve": 0.01,
  "objective": "working_kernel",
  "strict_autonomy": "true",
  "promotion_metric": "operator_e2e",
  "target_guards": ["8192_uniform"],
  "regression_guards": [
    "8192_rank-mixed-skew",
    "512_uniform",
    "512_rank-mixed-skew"
  ],
  "launch_target": 2,
  "require_overlap": "true",
  "require_attribution": "true",
  "require_artifact_distinct": "true",
  "accuracy_metric": "relL2",
  "accuracy_threshold": 0.10,
  "required_replays": 1000,
  "required_pairs": 5,
  "required_pairs_by_guard": {
    "8192_uniform": 5,
    "8192_rank-mixed-skew": 5,
    "512_uniform": 16,
    "512_rank-mixed-skew": 16
  },
  "_acceptance_comment": "This is a strict autonomous derivation run. Only 8192_uniform creates positive credit; the other guards can only veto. The operator rank-max e2e is the promotion metric across a launch-structure change. A terminal rung must reach two launches, controlled non-zero on-edge overlap, numeric accuracy, graph-safe 1000-replay liveness, complete attribution, and all guard evidence. objective=working_kernel keeps slower intermediate artifacts alive; it does not relax final acceptance.",
  "_state_comment": "Pass --state-dir pointing at a NEW empty directory for every strict proof run. The workflow refuses STATE_DIR/best inheritance because inherited code or manually edited state cannot prove derivation in this run.",

  "gpu_ids": "0,1,2,3,4,5,6,7",
  "gpus_per_job": 8,
  "_gpu_comment": "This is a collective: the group form of the lease takes all 8 cards as ONE lease. A round's GPU capacity is therefore one direction (roles/tech_lead.md rule 3e), and a single lease is capped by GEAK_GPU_RUN_TIMEOUT -- chunk the plan and re-emit results after every chunk.",
  "gpu_min_free_gib": 150,

  "use_expert_skills": "true",
  "analysis_skill": "none",
  "_analysis_skill_comment": "Deliberately none for the blind proof: the checked-in MoE analysis skill contains prior MegaMoE-specific post-mortem facts. The generic task-graph/resource-vector method remains available without pre-supplying this operator's conclusions.",
  "capability_eval": "true",
  "_capability_eval_comment": "Leave this true unless you are running the task for its output rather than evaluating the loop. Trusted bootstrap converts any --known-reference path to opaque path/content hashes and clears this field before agent startup.",
  "known_reference_paths": "${KNOWN_REFERENCE_PATHS}",

  "positive_control": {
    "name": "synthetic slowdown: injected fixed cost in the Stage2 tile loop",
    "how": "This control does NOT require a working optimization -- see roles/benchmark_engineer.md 5b, 'When no known-good change exists'. Build it in a workspace UNDER /tmp, outside the task tree, never where an engineer's path walk reaches. Candidate arm: add a bounded, side-effect-free spin (sized as described next -- do NOT size it off the published noise floor) to the Stage2 persistent tile loop, gated by an env var. Base arm: the same tree with the var unset. Because these kernels come from a caching JIT, the switch MUST be anchored per knowledge/jit_arm_isolation.md -- read it BEFORE authoring the gate, and record the two distinct disk_keys in activation_evidence. The candidate is the SLOWER arm, so measured_pct is expected NEGATIVE. SIZE IT AGAINST THE NULL, AND MEASURE THE NULL FIRST. The gate does not compare your effect to the guard's noise floor; it compares it to 3x the WORST PAIR in your own null arm, and the worst of a handful of pairs runs well above the floor. So run the null arm FIRST -- >=8 interleaved base-vs-base pairs on 8192_uniform, byte-identical trees -- take max(abs(null_pairs)), and aim the injection at >=4x that, leaving headroom for the sublinearity every spin knob has at small counts. Use a cheap single-rank dose ladder to hit the target, then commit to it: sizing against the measured null is required, tuning against the gate band afterwards is not allowed. Then run >=5 interleaved pairs on 8192_uniform with the spin on/off and report the paired effect as spin_on minus spin_off, together with the null pairs you collected first. Report `switch_present` about the knob YOU built, checked AFTER the gate is committed, against the tree you actually measured in -- this control names no pre-existing switch, so answering it before you start is meaningless and answering `false` would abort a valid control. Paste that grep into `switch_checked`. When it finishes, MOVE the control workspace aside (mv, not rm) and say in `note` where it was and that it is gone.",
    "expected_pct_lo": -9.0,
    "expected_pct_hi": -4.0,
    "magnitude": "constructed",
    "_magnitude_comment": "REQUIRED for this control, and the reason is a wave that died for want of it. The band above is a TARGET a synthetic knob was aimed at, not a previously measured fact -- nobody has ever measured 'the cost of this spin', because the spin did not exist until the run built it. Wave 7 built the best control this project has produced (-2.30%, 6 of 6 pairs the same sign, 6.6x the worst null pair, monotone across a four-level dose ladder) and was ABORTED for landing 0.2pp outside a band that was itself a guess about knob sizing. That is the wrong failure: a recorded win that reads low is an instrument problem, but an injected cost aimed at 3.4% that lands at 2.3% is a knob-sizing problem, and the instrument that measured it is the one part of the experiment that demonstrably worked. With magnitude=constructed the gate still requires the right sign, unanimity across pairs, and >=3x the WORST null pair -- it only tolerates landing above half the target. Do NOT relabel a recorded control as constructed to get past a failure; that converts the gate into a rubber stamp and there is no other instrument behind it.",
    "implausible_pct": 25.0,
    "guard": "8192_uniform",
    "abort_on_fail": true
  },
  "require_task_graph": true,
  "_require_task_graph_comment": "Analyze must emit the tile-level dependency DAG as a structured artifact (nodes, edges, edge scope, what enforces each edge today, critical path, slack, declared unknowns) before any direction is ranked -- see knowledge/tile_task_graph.md and the ANALYZE_SCHEMA task_graph field. This is the whole point of the wave: the question is not whether a fused kernel can be produced but whether the workflow can DERIVE it, and 'the stages are serialized, so fuse them' is a restatement of the launch count rather than a dependency analysis. The gate does not abort -- an honest partial graph beats a retry loop that returns a complete-looking invention -- it emits a caveat that travels to the report, so a missing or kernel-granular graph discredits the rankings instead of silently passing."
}
